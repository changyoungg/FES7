const person = {
  name: '재현',
  age: 20,
  gender: 'male'
};

console.log(Object.keys(person))

for(i=0; i<)

let input;

do {
  input = parseInt(prompt("숫자를 입력하세요."));
} while (isNaN(input));

console.log("입력한 숫자는 " + input + "입니다.");